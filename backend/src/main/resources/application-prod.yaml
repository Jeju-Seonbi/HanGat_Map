# 운영(OCI) 프로필 - 모든 비밀값은 서버 환경변수로 주입한다 (파일에 직접 기입 금지)
spring:
  application:
    name: hangat-api

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health
