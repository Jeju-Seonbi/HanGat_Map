import{describe,expect,it,vi,beforeEach}from'vitest';import{places}from'../../data/data';import{TourApiService,clearTourApiCache}from'./TourApiService';import type{TourApiClient}from'./TourApiClient'
describe('TourApiService',()=>{beforeEach(clearTourApiCache);it('returns null for no search result',async()=>{const client={get:vi.fn().mockResolvedValue([])}as unknown as TourApiClient;expect(await new TourApiService(client).findPlace(places[0])).toBeNull()});it('matches aliases and shares duplicate requests',async()=>{const get=vi.fn().mockImplementation((endpoint:string)=>endpoint==='searchKeyword2'?Promise.resolve([{contentid:'7',title:'제주 비자림'}]):Promise.resolve([]));const service=new TourApiService({get}as unknown as TourApiClient);const[a,b]=await Promise.all([service.findPlace(places[0]),service.findPlace(places[0])]);expect(a?.list.contentid).toBe('7');expect(b).toBe(a);expect(get).toHaveBeenCalledTimes(3)})})
