import{describe,expect,it}from'vitest';import{places}from'../../data/data';import{adaptTourPlace,coordinate,stripHtml}from'./TourApiAdapter'
describe('TourApiAdapter',()=>{const mock=places[0];it('merges real identity fields while preserving mock recommendation fields',()=>{const result=adaptTourPlace(mock,{list:{contentid:'1',title:'공식 비자림',addr1:'제주 주소',mapx:'126.8',mapy:'33.4',firstimage:'https://image.example/main.jpg'},detail:{overview:'<p>안전한 <b>설명</b></p>'},images:[]});expect(result).toMatchObject({contentId:'1',name:'공식 비자림',address:'제주 주소',longitude:126.8,latitude:33.4,imageSource:'TOUR_API',score:mock.score,level:mock.level});expect(result.description).toBe('안전한 설명')});it('uses additional image then mock fallback',()=>{expect(adaptTourPlace(mock,{list:{},images:[{originimgurl:'https://image.example/extra.jpg'}]}).image).toContain('extra');expect(adaptTourPlace(mock,{list:{},images:[]}).image).toBe(mock.image)});it('rejects invalid coordinates and strips overview HTML',()=>{expect(coordinate('0')).toBeUndefined();expect(coordinate('not-a-number')).toBeUndefined();expect(stripHtml('<script>x</script><p>제주&nbsp;숲</p>')).toBe('x 제주 숲')})})
