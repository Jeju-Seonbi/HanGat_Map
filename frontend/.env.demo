# 데모(포트폴리오) 빌드 전용 설정.
#
# 이 저장소에는 백엔드가 없고 api/ 전체가 목이라, 데모 계정 안내가 없으면
# 빌드 결과물에서는 로그인 자체를 시도할 수 없다.
# `npm run build:demo` / `npm run preview:demo` 로만 켜진다.
#
# ⚠️ 실서버가 붙으면 이 플래그로 빌드하지 말 것 — 자격증명이 화면에 그대로 노출된다.
VITE_DEMO=1
